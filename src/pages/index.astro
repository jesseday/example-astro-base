---
import Renderer from "@blocks/Renderer/ui/Renderer.astro";
import Layout from "@layouts/Layout.astro";

const content = [
  "hero",
  "duplex",
  "testimonial",
  "card list",
  "body",
  "video",
  "image",
];
---

<Layout title="Welcome to Astro.">
  {content.map((type) => <Renderer type={type} />)}
</Layout>
