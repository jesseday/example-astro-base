---
import Renderer from "@components/flow/Renderer.astro";
import Layout from "@layouts/Layout.astro";

import Section from "@components/ui/Section.astro";

const content = [
  "hero",
  "duplex",
  "testimonial",
  "card list",
  "body",
  "video",
  "image",
];
---

<Layout title="Welcome to Astro.">
  {content.map((type) => <Renderer type={type} />)}
</Layout>
