---
/**
 * @file Switch.astro
 * @description Passthrough component that renders
 *   a slot.  Matching is done by the `Match` component.
 *   Technically, this you could leave this component out
 *   when using Switch/Match, but I think it improves
 *   readability.
 * @see Match.astro
 */
---
<slot />